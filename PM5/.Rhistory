#remove.packages("keras")
install.packages("keras")
# Need to have a compatible version of tensorflow
tensorflow::install_tensorflow(version = "2.13.0")
#remove.packages("keras3")
#install.packages("keras3")
